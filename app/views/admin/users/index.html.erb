<% provide :title, t("index_pages.all_users") %>
<h1><%= t "index_pages.all_users" %></h1>

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>
